kind: capability
id: folders
title: Folders
app: folders
scope: Hierarchical folders for organizing entities (documents, links, projects) with tags, icons,
  colors, soft delete, and breadcrumbs, plus an LLM intelligence pipeline that extracts facts
  + embeddings from each folder's contents for chat-grounded retrieval. NOT the documents themselves
  (the Documents app owns those) — Folders organizes them.
state: live
autonomy: gated
links: {}
notes: ''
