"""Finance Calculator — a UI-only app package (compound interest + loan
amortization). All computation runs client-side in ui/FinanceCalcApp.jsx; there
is no backend data layer, routes, or schema."""
