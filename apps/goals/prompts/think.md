# PM Thinking Domain Prompt

> **Sub-chunk 3a:** placeholder. The real prompt content lands in
> sub-chunk 3e — moved from `prompts/PM_THINK.md` (currently at the
> platform root) and updated to use template variables for the primary
> user's display name + the household's user list.

The PM thinking domain runs on a schedule (default daily 9 AM) and
reviews open goals/projects/tasks. It uses this prompt as its system
message; output is a structured response (actions taken, memories
extracted, notifications fired).

For now see `prompts/PM_THINK.md` in the source repo for reference.
