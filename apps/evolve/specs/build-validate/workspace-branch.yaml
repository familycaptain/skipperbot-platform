kind: specification
id: evolve.build-validate.workspace-branch
title: Cut a feature branch into an isolated worktree
state: live
autonomy: hands-on
behavior: Starting work on an approved item cuts feature/<id> from `release` into a git worktree
  — an isolated checkout where an agent can write code without touching the running brain's
  code.
implements:
- apps/evolve/workspace.py
tests:
- type: unit
  path: apps/evolve/tests/test_workspace.py
links: {}
notes: ''
