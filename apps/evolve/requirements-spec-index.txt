# OPTIONAL — installed on box1 (the Evolve brain) ONLY, for Phase-2 cross-corpus spec
# retrieval in apps/evolve/spec_index.py (search_specs).
#
# Deliberately NOT in the base requirements.txt: a normal Skipper self-hoster must not be
# forced to install an embedding model for a brain-only feature (charter: build lean for the
# distributed self-hoster). spec_index lazy-imports this; if it's absent, cross-corpus
# retrieval is skipped and the Phase-1 capability-scoped read still applies.
#
# ONE pinned model, no fallback, so every install lands in the SAME vector space and the
# embedding cache is predictable/portable. The model name is pinned in code (`_EMBED_MODEL`
# in apps/evolve/spec_index.py = BAAI/bge-small-en-v1.5); change both together if it ever
# moves. fastembed = ONNX runtime, no torch.
fastembed>=0.3,<0.6
