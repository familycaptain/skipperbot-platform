"""Tests for this app. An app is a self-contained unit — code, UI, schema, migrations,
specs and tests travel together, so it can be installed, extracted to its own repo, or
removed in one piece. The package marker is required for `unittest discover` to descend here.
"""
