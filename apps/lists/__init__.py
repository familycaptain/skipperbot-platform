"""Skipperbot Lists App.

Owns lists + list_items data and the LLM-facing tools for managing them.

This is a required core app — the platform refuses to start without it
(the Todo app, several other apps, and chat all expect lists to exist).
"""
