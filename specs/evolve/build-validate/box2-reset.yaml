kind: specification
id: evolve.build-validate.box2-reset
title: Reset box 2 to a clean release baseline
state: live
autonomy: hands-on
behavior: Between runs box 2 resets to a clean `release` baseline (git reset --hard + clean)
  for reproducibility. Fixture-DB reload is the data half (§13), hooked separately.
implements:
- apps/evolve/workspace.py
tests:
- type: unit
  path: tests/evolve/test_workspace.py
links: {}
notes: ''
