kind: specification
id: evolve.build-validate.build-cycle
title: The implement -> validate -> merge cycle
state: live
autonomy: hands-on
behavior: 'The build cycle runs: serialize the approved spec, let implement write code in
  the worktree, commit, deploy to box 2, validate the bound tests; on green merge feature->release,
  and on any failure stop before merge.'
implements:
- apps/evolve/build_loop.py
tests:
- type: unit
  path: tests/evolve/test_build_loop.py
links: {}
notes: ''
