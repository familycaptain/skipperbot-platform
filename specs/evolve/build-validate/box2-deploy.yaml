kind: specification
id: evolve.build-validate.box2-deploy
title: Deploy the feature branch to box 2
state: live
autonomy: hands-on
behavior: Box 2 (a separate clone; an ssh remote in production) checks out the feature branch
  so the change can be validated in isolation, never on box 1 or production.
implements:
- apps/evolve/workspace.py
tests:
- type: unit
  path: tests/evolve/test_workspace.py
links: {}
notes: ''
