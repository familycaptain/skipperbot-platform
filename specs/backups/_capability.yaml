kind: capability
id: backups
title: Backups
app: backups
scope: Database + project-files backup runner. Two independently configurable destinations — a
  local/network filesystem path and Google Drive (service account with domain-wide delegation)
  — each with its own enable/disable toggle. Runs backups and tracks their status/history. NOT
  a restore/migration tool; Backups produces and tracks backup artifacts.
state: live
autonomy: gated
links: {}
notes: ''
