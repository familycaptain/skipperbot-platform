kind: capability
id: notifications
title: Notifications
app: notifications
scope: 'Cross-app notification fan-out: every ''tell the user X'' event is recorded as a notification
  row, then a background delivery loop pushes it out over the configured channels (Discord DM,
  Pushover, WebSocket, chat log, FCM mobile push). The shared delivery layer every app uses. NOT
  the message content owners (each app is) — Notifications owns recording + delivery.'
state: live
autonomy: gated
links: {}
notes: ''
