kind: specification
id: reminders.set.set-nag
title: Set a nag
state: proposed
autonomy: gated
behavior: 'A nag is a low-ceremony persistent daily nudge: it records a message (and an optional
  time-of-day slot) that re-fires every day until the user acknowledges or cancels it.'
implements:
- apps/reminders/tools.py
tests: []
links: {}
notes: 'Reverse-engineered draft (EVOLVE.md §12): describes current code behavior; needs human
  review + bound tests before `live`.'
