kind: capability
id: timeline
title: Timeline
app: timeline
scope: Family journal + microblog of posts, photos, and tags. Doubles as the platform's automatic
  activity log — whenever any app fires digest_record on a non-read CRUD, a personal post is written
  here, so every action is also a journal entry. NOT a per-app history view; Timeline is the unified
  family feed.
state: proposed
autonomy: gated
links: {}
notes: 'Reverse-engineered draft (EVOLVE.md §12): describes current code behavior; needs human
  review + bound tests before `live`.'
