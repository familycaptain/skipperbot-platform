# =============================================================================
# Skipperbot Platform — .env.example
# =============================================================================
#
# Copy this file to .env and fill in the REQUIRED values below.
# Everything in the OPTIONAL section is off by default and only needed
# to enable specific capabilities.
#
# Two REQUIRED values for the Docker path:
#   1. OPENAI_API_KEY        — your key, from https://platform.openai.com/api-keys
#   2. POSTGRES_PASSWORD     — pick any strong password
# Three for the native path: the above two, plus you must swap the active
# SKIPPERBOT_DB_DSN line below to the native one.
#
# See docs/01-base-platform-setup.md for the full setup walkthrough.
# See docs/03-extended-functionality.md for optional-integration setup.

# =============================================================================
# REQUIRED — fill these in before first boot
# =============================================================================

# ----- OpenAI API key -----
# Get one at https://platform.openai.com/api-keys (paid account).
OPENAI_API_KEY=

# ----- Postgres password -----
# Used in TWO places:
#   (a) The Docker `db` container creates Postgres with this password on
#       first boot (see docker-compose.yml).
#   (b) The same password must appear after `password=` in the
#       SKIPPERBOT_DB_DSN line below.
# Native-path users still need to set this for tooling consistency, even
# though Postgres itself is configured separately.
POSTGRES_PASSWORD=CHANGE_ME

# ----- Postgres connection string -----
# The agent uses this to log in. Pick ONE of the two lines below — keep
# exactly one uncommented. The password= field must match POSTGRES_PASSWORD
# above (they're literally the same string).
#
# DOCKER PATH (default — postgres runs as the `db` container in docker-compose).
# `host=db` is the docker-compose service name, NOT localhost.
SKIPPERBOT_DB_DSN=dbname=skipperbot user=skipperbot_user password=CHANGE_ME host=db port=5432
#
# NATIVE PATH (uncomment this line and comment out the Docker line above
# if you're running your own Postgres on the same host as the agent).
#SKIPPERBOT_DB_DSN=dbname=skipperbot user=skipperbot_user password=CHANGE_ME host=localhost port=5432

# =============================================================================
# OPTIONAL — uncomment + fill in to enable each capability.
# Skipperbot runs fine without any of these.
# See docs/03-extended-functionality.md for setup of each.
# =============================================================================

# ----- Timezone -----
# IANA name (e.g. America/Chicago, Europe/Berlin, Asia/Tokyo). Used for
# "now" in chat, reminder due times, schedule expansion, and date display.
# Defaults to Etc/UTC if unset, which means Skipper will schedule reminders
# in UTC and tell you "the time is X UTC" — set this to your local TZ.
# Find your name at https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# TIMEZONE=America/Chicago

# ----- Discord — chat with Skipperbot from your Discord server -----
# DISCORD_TOKEN=

# ----- Web search (Brave) — powers research jobs + knowledge crawls -----
# BRAVE_API_KEY=

# ----- Trello sync — mirror goals/tasks/lists to a Trello board -----
# TRELLO_KEY=
# TRELLO_TOKEN=

# ----- Outbound email (Resend) -----
# RESEND_API_KEY=

# ----- Gmail (inbound email rules + email app) -----
# Gmail inbound OAuth requires Tier 3 (external) deployment — see
# docs/03-extended-functionality.md "Going External".
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
# GMAIL_REDIRECT_URI=

# ----- Mobile push notifications (Firebase Cloud Messaging) -----
# FCM_SERVICE_ACCOUNT_FILE=

# ----- Pushover (alternative push channel) -----
# PUSHOVER_APP_TOKEN=
# PUSHOVER_USER_KEY=

# ----- Smart home (Home Assistant) -----
# HOME_ASSISTANT_URL=
# HOME_ASSISTANT_TOKEN=

# ----- Wake-word voice ("Hey Skipper") for skipperbot-voice -----
# PICOVOICE_API_KEY=

# ----- Backups to Google Drive (in addition to local backups) -----
# BACKUP_GOOGLE_KEY_FILE=
# GDRIVE_IMPERSONATE_EMAIL=

# ----- OpenAI budget tracking dashboard -----
# OPENAI_ADMIN_KEY=

# ----- Weather lookups (skipperbot-app-weather optional headless app) -----
# WEATHER_API_KEY=

# (POSTGRES_PASSWORD is in the REQUIRED section at the top.)

# =============================================================================
# DEPLOYMENT TIER (Tier 1 = localhost only, Tier 2 = LAN, Tier 3 = external).
# Leave both unset for the default Tier 1 install.
# =============================================================================

# Tier 2: set this to your LAN-visible URL so content links resolve from other devices.
# SKIPPER_LAN_URL=http://192.168.1.10:8000

# Tier 3: set this to your public URL (used for Gmail OAuth redirect, external access).
# SKIPPER_PUBLIC_URL=https://skipperbot.example.com

# =============================================================================
# DEVELOPMENT
# =============================================================================

# Bypass /api/* auth for requests originating from 127.0.0.1 / ::1. Off by default.
# DEV_ALLOW_UNAUTH_LOCALHOST=false
